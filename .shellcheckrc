# ShellCheck configuration for bash-scripts
# https://www.shellcheck.net/wiki/Ignore

# All scripts in this repo target bash
shell=bash

# Only fail on warnings and above (not style/info notes) — tighten to 'style' over time
severity=warning

# SC1091: Not following source — external files can't be statically analysed
disable=SC1091
